/**
 * Pacote responsável por armazenar os controllers do projeto.
 * Dentro de cada classe desse pacote há métodos que respondem a requisições HTTP,
 * ou seja, se comportam como servlets. Assim, uma request é feita e uma response
 * retornada com os devidos atributos e parametros preenchidos(POST) ou recebidos(GET).
 * Normalmente as respostas são páginas que podem ser requisitadas pelo nome. O retorno
 * dos métodos para o direcionamento das views podem ser String, ModelAndView etc.
 * 
 * As classes devem serguir o padrão NomeController com a extensão .java
 * 
 */
/**
 * @author Wellington Stanley
 *
 */
package br.poo.com.reqagile.controller;